本节学了一些基础的东西，包括创建一个简单的图和会话，以及在会话中执行图，其中要注意区分tf.Session.run()
和tf.Session.eval()的区别，run必须在sess持有的上下文才可以执行，而eval可以随时使用。然后还学了图的边和
节点，以及子图、多个图等一些小知识，还有当我们有多个计算设备时，可以将一个计算图分别用不同的设备来计算，
通过tf.device('/cpu:0')来切换。不同图、不同会话之间无法直接联系。使用图可以节省计算资源。
创建会话：
with tf.Session() as tf:
    sess.run(tf.add(3,5))